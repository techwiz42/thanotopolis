# backend/pytest.ini
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --cov=app
    --cov-report=term-missing
    --cov-report=html
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests

# backend/requirements-test.txt
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2
faker==20.1.0
factory-boy==3.3.0

# backend/test_commands.sh
#!/bin/bash

# Run all tests
echo "Running all tests..."
pytest

# Run only unit tests
echo "Running unit tests..."
pytest -m unit

# Run only integration tests
echo "Running integration tests..."
pytest -m integration

# Run with coverage
echo "Running tests with coverage..."
pytest --cov=app --cov-report=html

# Run specific test file
# pytest tests/unit/test_auth_service.py

# Run specific test
# pytest tests/unit/test_auth_service.py::TestAuthService::test_password_hashing
